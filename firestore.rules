rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to domains collection for registry display
    match /domains/{document} {
      allow read: if true;
      allow write: if false; // Only backend can write
    }
  }
}